# RestaurantOS Backend – required credentials
# Copy this file to .env and set values. Do not commit .env to git.

# MongoDB connection string (include username/password if using Atlas)
MONGO_URI=mongodb+srv://hamza:hamza123@cluster0.s2wseoe.mongodb.net/?appName=Cluster0

# Server port (dashboard should use NEXT_PUBLIC_API_BASE_URL pointing here)
PORT=5001

# Secret used to sign JWTs; must match dashboard JWT_SECRET for auth to work
JWT_SECRET=restaurantos_dev_secret_change_in_production_2026